/**
 * 1. Create firebase project
 * 2. create web app
 * 3. npm install firebase
 * 4. save firebase config and export default app
 * 5. Build> Authentication >get started >enable sign in method
 * 6. Create Sign up and Login route
 */

/**
 * 1- Create a context provider file
 * 2- Create context and set Provider
 * 3- set the children props
 * 4- set context value
 * 5- set the provider
 */

/**
 * Hosting in firebase
 * --------------------
 * One time per pc/laptop
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For each project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting firebase (up and down arrow) use space ber to select
 * 4. Exiting project
 * 5. Select the project carefully
 * 6. which project as public directory: dict
 * 7. Single page application: yes
 * 8. continue deployment: no
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 */